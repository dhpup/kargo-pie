apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: guestbook
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/dhpup/kargo-pie.git
      revision: HEAD
      directories:
      - path: env/*
  template:
    metadata:
      name: guestbook-{{path.basename}}
      annotations:
        kargo.akuity.io/authorized-stage: kargo-pie:{{path.basename}}
    spec:
      destination:
        namespace: guestbook-{{path.basename}}
        # Specify either a cluster destination name or server address:
        # name: REPLACEME
        # server: https://REPLACEME
      project: kargo-pie
      source:
        path: ./
        repoURL: https://github.com/dhpup/kargo-pie.git
        targetRevision: env/{{path.basename}}
